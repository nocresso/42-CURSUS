[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "Maze generator reusable module"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["mazegen"]